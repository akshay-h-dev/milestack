# Milestack - Flask Backend (In-memory)

This is a minimal Flask backend for Milestack. It uses an in-memory dictionary to simulate a NoSQL database and JWT-based authentication.

## Requirements

Install Python 3.10+ and run:

```bash
python -m pip install -r requirements.txt
